## Shopify Importer Frontend

### After cloning the project
    run -> npm install

### Commands to start the project
    run -> npm start
    and navigate to either localhost:3000 or [your-ip]:3000

### Start mock server
    run -> json-server --watch server/db.json --host [your-ip] --port 4500